_schema-version: "3.2"
ID: nszdemo00
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: nszdemo00-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: nszdemo00-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - nszdemo00.zip
      name: nszdemo00
      target-path: resources/
- name: nszdemo00
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: nszdemo00-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: nszdemo00-html5-srv
    service-plan: app-host
- name: nszdemo00-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: nszdemo00-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
